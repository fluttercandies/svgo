name: svgo_hooks_example
description: Example of using Dart Hooks to automatically optimize SVG assets during build
version: 1.1.0

environment:
  sdk: '^3.10.0'

dependencies:
  svgo: ^1.1.0
  path: ^1.8.0
  hooks: ^1.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0

publish_to: none